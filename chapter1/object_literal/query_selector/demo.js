// Single first by default p selector
const ptag = document.querySelector("p");

// If two errors are mentioned then we access from element type followed by class name
// const perror = document.querySelector("div.error");
// const pAll = document.querySelectorAll("p");

// const paresed = pAll.forEach((res) => {
//   console.log(res.innerHTML);
// });

// console.log(paresed);

// The Class Name gives HTMl collection whle the querySelector gives the
// const getElbyClass = document.getElementsByClassName("error");

// const convertInArr = Array.from(getElbyClass);
// console.log(convertInArr[0].innerHTML);
// For Overwriting the exsiting content from the latest one
// ptag.innerHTML = convertInArr[0].innerHTML;

// console.log();

// For Appending not Overwriting
// let newAddTextemail = convertInArr.forEach((result) => {
//   return (result.innerHTML += "ghanSSS");
// });

// console.log(newAddTextemail);
